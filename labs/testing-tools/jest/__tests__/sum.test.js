it('should add 1 + 2 to be equal to 3', () => {

  // o "expect" "toBe" é chamado de matcher
  // Documentação com os demais matchers disponíveis: https://jestjs.io/docs/en/using-matchers
  expect(1 + 2).toBe(3);

});
